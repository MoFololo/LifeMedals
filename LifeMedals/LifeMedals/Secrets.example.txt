// Secrets.swift 模板
//
// 使用方法：
// 1. 复制本文件内容
// 2. 在 Xcode 中，于 LifeMedals/LifeMedals/ 目录下新建文件 "Secrets.swift"（会自动被识别，无需手动加入 target）
// 3. 粘贴以下内容，并替换成 Supabase Dashboard → Project Settings → API 中的真实值
// 4. Secrets.swift 已在 .gitignore 中，不会被提交到 git

import Foundation

enum Secrets {
    static let supabaseURL = URL(string: "https://YOUR_PROJECT_ID.supabase.co")!
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"
}
