# LifeMedals - 环境变量说明
# 使用方法：cp .env.example .env，然后填入真实值
# ⚠️  .env 已在 .gitignore 中，绝对不要把真实值提交到 git
#
# v1 客户端使用 SwiftData 纯本地存储，不再需要 Supabase 配置。
# 唯一需要联网的部分是无状态 AI 代理（Cloudflare Workers / Vercel Functions），
# 代理只转发请求，不存储任务、图片或用户数据。

# ------------------------------------------------------------------
# Anthropic（仅用于无状态 AI 代理，禁止写入客户端代码）
# 在 https://console.anthropic.com/ 中获取
# 部署时通过代理平台（Cloudflare Workers / Vercel Functions）的
# 环境变量/Secrets 功能写入，不进客户端 Swift 代码
# ------------------------------------------------------------------

ANTHROPIC_API_KEY=

